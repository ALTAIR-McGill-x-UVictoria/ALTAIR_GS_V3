fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pyserial>=3.5
websockets>=12.0
pynmea2>=1.19.0
python-dotenv>=1.0.1
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# Telescope mount + camera integration (see backend/lib/README.md for the
# non-pip binaries these need: ASI SDK shared library, ASCOM Platform).
# Captures are saved as FITS (astropy.io.fits) — the standard astronomical
# image format — with metadata in the header instead of EXIF. Pillow is
# still needed for the gallery's FITS-to-JPEG preview generation.
nexstar>=0.0.5
zwoasi>=0.2.0
astropy>=6.0.0
Pillow>=10.0.0
pywin32>=306; sys_platform == "win32"

# Direct AM3/AM5 mount control over INDI (Linux-friendly alternative to the
# ASCOM/pywin32 path above — see backend/lib/README.md). backend/indi_client.py
# speaks INDI's XML wire protocol directly with the standard library, no
# extra pip package needed — but it requires a local `indiserver` running
# `indi_lx200am5` (system package, e.g. `indi-full` on Debian/Ubuntu).

# Canon EOS Rebel T3i test path (backend/camera_canon.py), selected via
# ALTAIR_CAMERA_TYPE=canon — an alternative to the ZWO ASI585MC being
# evaluated for its larger APS-C sensor. Requires libgphoto2 installed
# system-wide first (e.g. `apt install libgphoto2-dev` on Linux); this
# package only provides the Python bindings. See backend/lib/README.md.
gphoto2>=2.5.0; sys_platform != "win32"
