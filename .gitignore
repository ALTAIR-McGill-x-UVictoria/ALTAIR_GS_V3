# JS dependencies — reinstall with: npm install
node_modules/

# Vite build output
dist/
dist-ssr/

# Python
__pycache__/
*.pyc
.venv/
.env
.env.local

# Runtime binaries (obtain separately — see backend/lib/README.md)
backend/lib/*.dll
backend/lib/*.so
backend/lib/*.so.*
backend/lib/*.dylib

# Telemetry log sessions (keep the logs/ dir itself via .gitkeep)
logs/*/

# Editor
.vscode/
.idea/
.claude/
